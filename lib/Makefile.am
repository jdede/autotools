noinst_LTLIBRARIES = libtest.la
# internal cpp and h files, not public interface
libtest_la_SOURCES = testlib.cpp
# Maybe include math lib (in addition to include <math.h>)
#libtest_la_LIBADD = -lm
# public interface
#include_HEADERS = testlib.h
